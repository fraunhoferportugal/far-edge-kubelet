# Integration testing
This directory will host `far-edge-kubelet` integration tests. Those are tests asserting how `far-edge-kubelet` componets interact with external systems. To do so, it is not required to use the complete infrastructure stack and mock services can be used.
